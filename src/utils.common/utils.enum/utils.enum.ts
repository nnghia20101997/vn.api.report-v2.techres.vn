export enum Percent {
   MIN = 0,
   MAX = 100
}

export enum ValueType {
   NUMBER = 0,
   STRING = 1
}

export enum TypeUser {
   ADMIN = 3,
   SALER = 5
}

export enum TypeSalerLog {
   TYPE_MAX = 4,
   TYPE_MIN = -1,
}

export enum ResetPassword {
   NEW_PASS = '0000'
}